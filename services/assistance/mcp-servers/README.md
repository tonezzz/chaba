# MCP servers

This folder is reserved for MCP server services.

Conventions:
- One folder per server: `mcp-servers/<server-name>/...`
- Internal-only by default.
- Each server declares its own persistence volumes/bind mounts.
