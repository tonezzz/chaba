# IDC1 Health Check Configuration
# Copy this file to .env and fill in the values below

# Required: SSH connection details for idc1
SSH_USER=your_username_here
SSH_HOST=idc1.hostname.or.ip
SSH_KEY_PATH=/path/to/your/ssh/private_key

# Optional: Customize these if your setup uses different ports
# SSH_PORT=22
# MCP0_PORT=8355
# MCP_AGENTS_PORT=8046
# MCP_DEVOPS_PORT=8425

# Instructions:
# 1. Copy this file to .env: cp .env.template .env
# 2. Edit .env and replace the placeholder values with your actual configuration
# 3. Make sure the SSH private key has the correct permissions: chmod 600 <keyfile>
# 4. Run the health check: ./scripts/idc1-health-sweep.sh
