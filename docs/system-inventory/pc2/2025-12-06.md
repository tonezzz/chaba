# pc2 Inventory — 2025-12-06

## Summary
- **Role**: Windows 11 Pro workstation (HP 15-BC) acting as delegated worker via WSL2.
- **Primary users**: `hp` (Windows), `tonezzz` and `chaba` (WSL/SSH).
- **Docker**: Desktop/Engine `28.5.2` (WSL backend). Daemon currently idle after full `docker system prune -af` on Dec 6.
- **SSH**: Key-based access via `/home/tonezzz/.ssh/chaba_ed25519`. Port 22 forwarded from Windows to WSL with Windows `sshd` disabled.
- **Stacks**: `base-tools` compose profile running (`node-runner`, `python-runner`, `redis`). Dev proxy certs generated via mkcert (Dec 6) under `stacks/pc2-worker/dev-proxy/certs/`.

## Hardware (Windows Host)
| Component | Details |
| --- | --- |
| Model | HP 15-BC |
| CPU | Intel Core i7-9750H (6 cores / 12 threads @ 2.6 GHz) |
| RAM | 32 GB (2×16 GB, 2667/3200 MHz) |
| GPU | NVIDIA GTX 1650 (driver 32.0.15.7703) + Intel UHD 630 (26.20.100.7927) |
| Storage | C: 323 GB (TONY_WIN); D: 1 TB (KK DATA, nearly full) |
| Network | Intel Dual Band Wireless-AC 7265 (DHCP 192.168.1.39), Realtek GbE (disconnected) |

## Windows OS
- Version: Windows 11 Pro 23H2 (10.0.26100).
- Installed: 2025‑07‑26.
- Security: VBS + Hypervisor-enforced code integrity enabled.
- Power: `powercfg /change standby-timeout-ac 0` and `/change hibernate-timeout-ac 0` applied (no sleep on AC).

## WSL Ubuntu 24.04.2 LTS (`tonezzz`)
| Item | Value |
| --- | --- |
| Kernel | 5.15.167.4-microsoft-standard-WSL2 |
| Resources | 15 GiB RAM assigned, 1 TB virtual disk (`/dev/sde`, 23 GB used / 934 GB free) |
| SSH | `sshd` enabled via systemd; port 22 forwarded through Windows `netsh` portproxy (Windows `sshd` disabled) |
| Key users | `tonezzz` (owns `/home/tonezzz`), `chaba` (sudo NOPASSWD) |

## Docker / Container Stack
- Engine: `Docker version 28.5.2, build ecc6942`.
- Current state: no running containers; all unused containers/images pruned (Dec 6, 2025).
- Running profile (Dec 6): `base-tools` (redis, node-runner, python-runner). Monitoring profile disabled (WSL mount limitation). MCP profile paused until needed.
- Next steps: scaffold `~/stacks/pc2-worker/docker-compose.yml` with profiles (`base-tools`, `mcp-suite`, `gpu-inference`).

## Networking & Access
- Windows host reachable at `pc2` / `192.168.1.43`.
- Port 22 forwarded to WSL IP (currently `172.31.232.84`). `netsh interface portproxy` entry confirmed.
- SSH fingerprints:
  - Host key (WSL sshd): `SHA256:va6/T6/W5hMETZ/3RBj5Qt8r1ZRRAyifGC7qp4J6moY`.
  - Stored in `/home/tonezzz/.ssh/known_hosts`.

## Outstanding Actions
1. Create `docs/system-inventory/pc2/README.md` with update procedure (optional).
2. Set up `~/stacks/pc2-worker/docker-compose.yml` and supporting `.env`.
3. Consider enabling GPU pass-through for CUDA workloads once NVIDIA drivers in Windows are updated.
4. Add `chaba` and `tonezzz` to `docker` group (currently using `sudo docker …`).
