title: Focus Inbox Item
subtitle: One new focus, task, or idea to triage
icon: inbox

focus:
  label: 2026-09-10 session handoff — devin cleanup + barrier image copy
  text: |
    Session handoff from 2026-09-10. The Devin session grew too large; this item captures
    pending work and learnings.

    Completed:
    - michael-dev HA-MCP is now healthy: port 9585 responding, 172 entities via MCP.
    - All 3 HA MCPs verified: home-assistant (211), michael-ha (1,337), michael-dev (172).
    - SSOT updated: auth/display names synced for michael-dev and michael-ha.
    - ssot.home-assistant.instances.yml status block refreshed to 2026-09-10.
    - Commits b2f7aa21 and 4e800abd pushed to origin/master.

    Pending work:
    - devin-cleanup.timer on tony-omen failed at 03:24: `No space left on device` while
      copying 155G sessions.db to backups. Need more space or modify the script to skip/relocate
      the backup.
    - Barrier image copy/paste (tony-omen → tony-dell) is a known Barrier 2.4.0 Linux
      limitation; needs a workaround or replacement.
    - mcp-health active alert: `Home Assistant Live (Gemini Live bridge)` HTTP 502 since
      2026-09-06 22:47.

    Barrier workaround options:
    1. Replace Barrier with input-leap / patched build on both hosts.
    2. Shared-clipboard folder + hotkey scripts (sync image via ssh/tailscale).
    3. Enable Barrier drag-and-drop and drop image files instead of paste.

    Devin cleanup options:
    1. Free ~155G on /home so the backup+vacuum can complete.
    2. Modify scripts/prune-devin-sessions.sh to skip the backup when disk is low.
    3. Move the DB to a larger disk, prune/vacuum, then move back.
  branch: master
  priority: medium
  status: active
  tags:
    - handoff
    - devin-cleanup
    - barrier
    - home-assistant
    - mcp
  missing_info:
    - Which Barrier image-copy approach to use (replace vs shared folder vs drag-and-drop).
    - Whether to modify prune-devin-sessions.sh to skip backup or free space first.
  safe_to_parallel:
    value: false
    reason: Cleanup involves deleting/large file moves; Barrier fix affects keyboard/mouse flow.
  subtasks:
    - label: Devin cleanup — free space or modify prune-devin-sessions.sh to skip backup
      status: not_started
    - label: Barrier image copy — pick workaround and implement
      status: not_started
    - label: Investigate mcp-health alert — Home Assistant Live (Gemini Live bridge) HTTP 502
      status: not_started
    - label: michael-dev HA-MCP — done; verify overnight and document if needed
      status: completed

ownership:
  owner: tony
  session: ''
  locked: false
  lock_reason: ''

source:
  session: 'current'
  date: 2026-09-10
