title: Home Assistant Dashboards
subtitle: Tony-test dashboard layout, tabs, and active test views

instances:
  dev:
    name: tony-test
    instance_ssot: ssot.home-assistant.michael.dev.yml
    base_url: https://tony-dell.taila0626a.ts.net:8124/tony-test
    active_view: V0
    storage_path: /home/tony/.config/michael-dev/.storage/lovelace.tony_test
    repo_snapshot: docs/home-assistant/dashboards/tony-test-current.json
    note: >-
      The live Lovelace layout is stored in .storage/lovelace.tony_test. The
      repo JSON is an archival snapshot. 2026-09-13: Cast tab removed from
      dev after being promoted to live michael-ha. 2026-09-08: PF3/PF4/PFG/PFG1/PFG2
      removed; current layout is
      V0/V1/G1/TPL/TPL2/TPL_SOLIS/Data/Sankey/juWorkshop/Solar
      Assistant/glass/Weather/Solis Daily Energy Sankey.

  live:
    name: tony-test
    instance_ssot: ssot.home-assistant.michael.yml
    base_url: https://nupo4ndqdqydt78zmpq0z5wzp1bdrqgs.ui.nabu.casa/tony-test
    active_view: V0
    storage_path: /config/.storage/lovelace.tony_test
    repo_snapshot: docs/home-assistant/dashboards/michael-ha-tony-test-current.json
    note: >-
      The live michael-ha tony-test dashboard. 2026-09-14: Cast tab removed from
      live michael-ha tony-test. 2026-09-08: PFG2 tab removed; current layout is
      V0/V1/SK/TPL/juWorkshop/Solar Assistant/glass/Weather.

  chaba_admin:
    name: chaba-admin
    instance: tony-ha
    title: Chaba admin
    icon: mdi:shield-crown
    url: https://tony-dell.taila0626a.ts.net:8123/chaba-admin/pair
    require_admin: true
    created: "2026-09-20"
    storage_path: /home/tony/.config/home-assistant/.storage/lovelace.chaba_admin
    repo_snapshot: docs/home-assistant/dashboards/chaba-admin-current.json
    note: >-
      Sidebar dashboard (storage mode) hosting the Ada HA pair panel natively —
      no iframe. Backing config lives in tony-ha YAML:
      input_text.ada_pair_device_name / ada_pair_redeem_url,
      input_select.ada_pair_instance, scripts ada_pair_issue / ada_pair_revoke
      (scripts.yaml), rest_command ada_issue_key / ada_revoke_key,
      shell_command ada_pair_save_qr, rest sensors ada_*_issued_keys (all in
      configuration.yaml), secret ada_admin_key in secrets.yaml. QR SVG is
      written to /config/www/pair-qr.svg and shown in a picture card. Admin key
      stays server-side in secrets.yaml.
      Issued-keys card is custom:ada-keys-card (/local/ada-keys-card.js?v=4,
      repo: stacks/tony-dell/tony-ha/www/ada-keys-card.js) — each key row is a
      plain label plus four buttons: Voice and Text re-pair via callWS
      return_response (script returns resp via stop+response_variable) and
      open the fresh redeem link in a synchronously-opened popup (iOS-safe),
      consuming the single-use token in the admin browser; Re-pair refreshes
      the QR without opening; Revoke calls script.ada_pair_revoke_named.
      ada_pair_reissue takes a ui field (voice|chat) and calls
      rest_command.ada_repair_key → POST /api/auth/keys/{name}/redeem on mn01,
      which preserves key identity/audit history and accepts a `redirect`
      field: ui=chat lands on /apps/ha/ada-{instance}/chat (the Ada text-chat
      page in ada-pi pwa/chat/, served by pwa_server StaticFiles) instead of
      the voice PWA root.
      QR display is custom:ada-pair-qr-card (/local/ada-pair-qr-card.js, same
      repo dir) — renders /local/pair-qr.svg with a ?v= cache-buster derived
      from the redeem token (stock picture card caches the URL forever).
      Users card is custom:ada-users-card (/local/ada-users-card.js, same repo
      dir) — lists HA auth users via config/auth/list (system_generated
      filtered out), per-user key name user-<username>; users without a key
      get an Issue button that sets the shared pair inputs and runs
      script.ada_pair_issue (QR lands in the QR card), keyed users get a
      QR-icon button that opens an overlay with the redeem QR plus a
      clickable link that pops up the HA web UI (/chaba-home/ai) —
      Voice/Text/Re-pair/Revoke buttons removed 2026-09-23.
      Events tab added 2026-09-20: custom:chaba-events-card
      (/local/chaba-events-card.js, repo: stacks/tony-dell/tony-ha/www/) reads
      /local/chaba-events.json (written by /config/scripts/chaba-event-log.py —
      flock, 24h prune, 300 cap, `ack` sets responded) plus live
      persistent_notification entities (always requires-response). Shared
      filter state in input_text.chaba_events_filter ({"hidden": [...],
      "log_hidden": [...]} — both cards merge-write the shared JSON).
      Log tab added 2026-09-21: `type: panel` view with a single
      custom:chaba-log-card (/local/chaba-log-card.js?v=4, same repo dir) —
      an isolated copy of the built-in /logbook page so its UX/UI can be
      modified freely. It renders the same <ha-logbook> element +
      logbook/event_stream backend as the native page (force-loaded via a
      throwaway hui-logbook-card from loadCardHelpers), behind our own
      toolbar: Live 1h/24h/7d presets ({recent: seconds} = live tailing),
      Custom datetime range ({range:[Date,Date]} = fixed window), refresh,
      entity text filter, and the per-domain checkbox row. The list scrolls
      inside the card; vertical fit is JS-measured (parent content-box
      bottom clamped to visualViewport — innerHeight under-counts on iOS
      when the browser toolbar is visible), refit on resize/hass. The card
      renders light-DOM so :host doesn't apply — selectors are scoped via
      the tag name and clc-* classes; CSS height:100% is the fallback. Filter groups are entity domains (input_* grouped as
      "input"), persisted as "log_hidden" in the same input_text; toggling a
      group rebuilds the inner card's entities list. The inner card's chrome
      is flattened via an injected shadowRoot <style>. Events card v4 dropped
      the "logbook" checkbox (it gated a card on another tab);
      input_boolean.chaba_events_show_logbook is now vestigial — nothing
      reads it. Note HA 2026.8 logbook cards error
      ("target has no entities") without entities:/target:, so it carries a
      curated entity list (pairing/cast scripts+automations, helpers, cast
      devices, ada sensors). Producers: shell_command.chaba_event /
      chaba_event_ack (base64 JSON payload), called from ada_pair_* scripts;
      deploy-ada.sh emits over SSH. Events with requires_response && !responded
      pin to top with a pulsing border (prefers-reduced-motion respected);
      confidence <0.6 shown as outlined chip.
      Voice card added 2026-09-22 at the top of the Pair tab, moved off it
      2026-09-23 (pair tab is admin-only; the live mic lives on
      chaba-home/ai):
      custom:ada-voice-card (/local/ada-voice-card.js?v=4, repo:
      stacks/tony-dell/tony-ha/www/ada-voice-card.js) — embeds the full Ada
      Gemini Live client in the dashboard, no iframe/redirect. It ports the
      PWA transport (ada-pi pwa/app.js): getUserMedia -> ScriptProcessor
      downsamples to PCM16@16kHz over WebSocket; server PCM16@24kHz plays
      through a blob-URL AudioWorklet (PCMPlayer); JSON control messages
      (ready/transcripts/response_*/live_reconnecting/clear_audio) drive the
      status line. Connects to wss://mn01.taila0626a.ts.net/apps/ha/ada-tony/ws
      with device_id + api_key query params — websocket_authorized accepts
      the api_key param so no CORS/REST calls are needed. Key provisioning
      is automatic: on first connect the card calls script.ada_voice_key via
      hass.callWS (return_response) — the script creates-or-repairs a
      per-device issued key ha-<device8> (rest_command.ada_issue_key /
      ada_repair_key), then burns the redeem token server-side via
      rest_command.ada_voice_redeem (POST /api/auth/redeem with
      token+expect_name+device_id, NO api-key header — a caller key would
      override expect_name and fail) and returns the raw key, which the
      card caches in localStorage ada_voice_api_key. First WS connect TOFU-
      binds the key to the browser's device_id; a 4401 close (revoked /
      foreign binding) triggers one re-mint retry, then falls back to a
      paste-once unlock field. v3 (2026-09-23): controls column added —
      a Mute button hard-mutes the mic (track disabled), and an "Auto"
      checkbox (persisted in localStorage ada_voice_auto_mute) VAD-gates
      the mic: after local_speech_stopped the mic is auto-muted (captured
      for VAD but not sent) and speaking again auto-resumes. v4 (2026-09-23):
      the card publishes status transitions + You/Ada transcripts to a shared
      window channel consumed by custom:ada-activity-card (same bundle) — a
      log-style live feed used on chaba-home's AI tab. The admin key never leaves HA (secrets.yaml),
      no key sits in dashboard config (lovelace snapshots sync into this
      repo), and every browser gets its own revocable issued key visible in
      the ada-keys-card list. Mic needs HTTPS (satisfied via Tailscale
      serve :8123) and a user gesture for AudioContext (the mic-button
      tap). Card disconnects mic + socket in disconnectedCallback when the
      view is left.
      Direction (decision ada-ha-native-primary-ui, 2026-09-23): HA is the
      primary user-facing Ada UI — users reach Ada from inside the HA app so
      there is one auth/session surface and less to secure. The standalone
      ada-pi PWA frontend is a staged retirement candidate; backend API/WS
      routes stay because these cards still use them.
    tabs:
      - title: Pair
        path: pair
        icon: mdi:qrcode-plus
        cards: ada-keys-card, ada-users-card (QR-icon re-pair popup), capabilities info — voice card, issue/revoke form, and pairing-QR card removed 2026-09-23 (voice lives on chaba-home/ai; per-user QR popups supersede the shared pair form)
      - title: Events
        path: events
        icon: mdi:bell-badge-outline
        cards: chaba-events-card
      - title: Log
        path: log
        icon: mdi:history
        type: panel
        cards: chaba-log-card (full-height logbook 24h, own domain filters)
      - title: Ada Pages
        path: ada-pages
        icon: mdi:file-document-edit-outline
        type: panel
        cards: iframe → https://mn01.taila0626a.ts.net/apps/ha/ada-tony/cms/?api_key=...
          (dedicated ada-ha-tony key 'cms-viewer' on mn01; key is embedded in the
          URL because iframes can't set headers — it is revocable via the Pair
          tab. The key is a shared viewer key (device='*' in the keys file),
          so it works from any browser without binding. Added 2026-09-22; the
          api_key param was restored 2026-09-23 via push-dashboard.py websocket
          mutate — a HA restart had flushed its in-memory config over the
          earlier .storage edit. Use push-dashboard.py, not .storage edits.)

  chaba_home:
    name: chaba-home
    instance: tony-ha
    title: Chaba home
    icon: mdi:home-heart
    url: https://tony-dell.taila0626a.ts.net:8123/chaba-home/ai
    require_admin: false
    depends_on:
      - custom:ada-voice-card (/local/ada-voice-card.js — same card and
        auto-minted per-device key flow documented under chaba_admin above)
    created: "2026-09-22"
    storage_path: /home/tony/.config/home-assistant/.storage/lovelace.chaba_home
    repo_snapshot: docs/home-assistant/dashboards/chaba-home-current.json
    note: >-
      Sidebar dashboard (storage mode) created via websocket
      lovelace/dashboards/create on 2026-09-22. First view reuses the tony-test
      "g3" page template (panel view, single custom:pfg3d-card with a bar3d
      chart, pfg_transparent + pfg_fit_screen) pointed at the Tuya Nobito air
      sensor (sensor.nobito_pm2_5, µg/m³) instead of PV power. The dev-only
      entity_input helper (input_select.dev_g3_source) was dropped — the entity
      is hardcoded. IMPORTANT: custom:pfg3d-card is NOT part of the
      sunsynk-power-flow-card bundle — it is a standalone bundle. Deployed
      2026-09-23: /local/pfg3d-card-v239.js?v=1 (module resource, copied from
      michael-dev www; v206-era sunsynk bundle only contains the
      pfg3d-hover-plane helper, so the view showed "configuration error" until
      this resource was added). echarts-5.5.1.min.js +
      echarts-gl-2.1.0.min.js were vendored into tony-ha /local/ (copied from
      michael-dev www) so the 3D chart loads local-first, CDN fallback. Nobito is a
      battery Tuya device (cloud integration) that sleeps between reports —
      entities show unavailable while asleep; a flat card value means stale
      data, not a real reading. 2026-09-23: ada-voice-card added at the top —
      the panel view's single card is now a vertical-stack (voice card, then
      the pfg3d card); the pfg3d card title was removed and pfg_fit_screen
      raised 120px → 290px to keep the chart inside the viewport below the
      voice card. Later 2026-09-23: split into two tabs — "AI" (path ai,
      mdi:robot-outline) holds the voice card (titled "Live assistant") plus
      a new custom:ada-activity-card ("Live activity", defined in the same
      ada-voice-card.js bundle v4) that renders a log-style live feed of
      status transitions + You/Ada transcripts from a shared window channel
      (300-entry cap, auto-scroll, dedupes repeats); "Nobito" (path nobito,
      mdi:poll) holds the pfg3d chart alone with pfg_fit_screen back at
      120px. Later 2026-09-23: the bar3d def sets stat:"range" — bars float
      from hourly min to hourly max (stacked bar3D: transparent base=min,
      delta=max−min, colored by hi via visualMap dimension 4, tooltip shows
      "lo–hi"). Requires the in-place patched pfg3d-card-v239.js (resource
      ?v=2) — see cards SSOT.
    tabs:
      - title: AI
        path: ai
        icon: mdi:robot-outline
        type: panel
        cards: vertical-stack of ada-voice-card ("Live assistant") +
          ada-activity-card ("Live activity" log feed)
      - title: Nobito
        path: nobito
        icon: mdi:poll
        type: panel
        cards: pfg3d-card bar3d stat=range on sensor.nobito_pm2_5 (10-day
          hour×day floating lo–hi bars, no title)

tabs:
  dev:
    - order: 0
      title: V0
      path: p0
      note: New first tab (sunsynk card).
      test_url: https://tony-dell.taila0626a.ts.net:8124/tony-test/p0
    - order: 1
      title: V1
      path: p0-2
      note: Second sunsynk card view.
      test_url: https://tony-dell.taila0626a.ts.net:8124/tony-test/p0-2
    - order: 2
      title: G1
      path: g1
      note: >-
        30-day 3D surface of sensor.inverters_1_pv_power (PV power, 0-10kW,
        scale 0.001) rendered with pfg_charts type 'surface3d' (echarts-gl,
        vendored /local libs, hover plane + click-to-pin).
      test_url: https://tony-dell.taila0626a.ts.net:8124/tony-test/g1
    - order: 3
      title: TPL
      path: tpl
      note: Template/pfg card experiment.
      test_url: https://tony-dell.taila0626a.ts.net:8124/tony-test/tpl
    - order: 4
      title: TPL2
      path: tpl2
      note: Template variant 2.
      test_url: https://tony-dell.taila0626a.ts.net:8124/tony-test/tpl2
    - order: 5
      title: TPL_SOLIS
      path: tpl_solis
      note: Solis template view.
      test_url: https://tony-dell.taila0626a.ts.net:8124/tony-test/tpl_solis
    - order: 6
      title: Data
      path: data
      note: Mock data controls for dev testing.
    - order: 7
      title: Sankey
      path: solis-daily-energy-sankey
    - order: 8
      title: juWorkshop
      path: juworkshop
    - order: 9
      title: Solar Assistant
      path: ~
    - order: 10
      title: glass
      path: ~
    - order: 11
      title: Weather
      path: weather
    - order: 12
      title: Solis Daily Energy Sankey
      path: ~

  live:
    - order: 0
      title: V0
      path: p0
      test_url: https://nupo4ndqdqydt78zmpq0z5wzp1bdrqgs.ui.nabu.casa/tony-test/p0
    - order: 1
      title: V1
      path: p0-2
      test_url: https://nupo4ndqdqydt78zmpq0z5wzp1bdrqgs.ui.nabu.casa/tony-test/p0-2
    - order: 2
      title: G1
      path: g1
      note: >-
        30-day 3D surface of sensor.inverters_1_pv_power (PV power, 0-10kW,
        scale 0.001, surface3d), copied from michael-dev and placed behind V1.
      test_url: https://nupo4ndqdqydt78zmpq0z5wzp1bdrqgs.ui.nabu.casa/tony-test/g1
    - order: 3
      title: SK
      path: solis-daily-energy-sankey
      note: Sankey chart dashboard.
      test_url: https://nupo4ndqdqydt78zmpq0z5wzp1bdrqgs.ui.nabu.casa/tony-test/solis-daily-energy-sankey
    - order: 4
      title: TPL
      path: tpl
      test_url: https://nupo4ndqdqydt78zmpq0z5wzp1bdrqgs.ui.nabu.casa/tony-test/tpl
    - order: 5
      title: juWorkshop
      path: juworkshop
    - order: 6
      title: Solar Assistant
      path: ~
    - order: 7
      title: glass
      path: ~
    - order: 8
      title: Weather
      path: weather

manual_steps:
  - Preferred: mutate the live dashboard via scripts/home-assistant/push-dashboard.py (websocket lovelace/config/save; applies immediately, no restart)
  - Direct .storage/lovelace.tony_test edits only take effect after an HA core restart

  - Add a sidebar dashboard: websocket lovelace/dashboards/create (url_path must contain a hyphen, e.g. dossier); created 2026-09-06 as 'Dossier' (mdi:folder-file).
  - Sidebar order is per-user in .storage/frontend.user_data_<uid> under the 'sidebar' key (panelOrder array); set via websocket frontend/set_user_data with key='sidebar'. Current order: lovelace (Overview), tony-test, dossier, then defaults. michael-dev already had dossier-home under tony-test; michael-ha was set 2026-09-08 to place dossier under tony-test.

  - To snapshot, run scripts/home-assistant/sync-ssot-from-live.sh (copies .storage to docs/home-assistant/dashboards/tony-test-current.json and refreshes bundle version)
  - Tab order, titles, and paths live in the same .storage file / saved config
  - To add a new tab, push via websocket mutate or use the HA UI, then snapshot

status:
  last_updated: "2026-09-23"
  last_verified: "2026-09-23"
  health: active; G1 on both hosts (dev v171 / ha v161, same md5 b404aab0) — surface3d PV chart on sensor.inverters_1_pv_power, vendored echarts libs in /local; Cast tab removed from live michael-ha tony-test on 2026-09-14; PF3/PF4/PFG/PFG1/PFG2 removed
