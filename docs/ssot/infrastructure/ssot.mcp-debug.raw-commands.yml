title: MCP Debug Raw Commands
subtitle: Allowed raw command prefixes for mcp_raw — split from ssot.mcp-debug.yml
icon: bug
source: docs/ssot/infrastructure/ssot.mcp-debug.yml
split_at: '2026-09-18'

raw_commands:
  allowed_prefixes:
  - systemctl
  - podman
  - docker
  - python3
  - df
  - ps
  - free
  - uptime
  - mount
  - lsblk
  - lsusb
  - journalctl
  - ss
  - env
  - top
  - lsof
  - launchctl
  - nvidia-smi
  - rocm-smi
  - tailscale
  - ip
  - tail
  - head
  - cat
  - ls
  - which
  - find
  - whereis
  - locate
  - grep
  - sed
  - awk
  - xargs
  - sort
  - uniq
  - cut
  - wc
  - git
  - du
  - whoami
  - id
  - uname
  - hostname
  - lspci
  - lsmod
  - modinfo
  - lsmem
  - lscpu
  - jq
  - yq
  - dig
  - nslookup
  - host
  - resolvectl
  - systemd-analyze
  - file
  - stat
  - readlink
  - realpath
  - lsb_release
  - vm_stat
  - launchctl
  - diskutil
  - system_profiler
  - scutil
  - netstat
  - lsof
  - pgrep
  - pkill
  - nohup
  - brew
  - xcode-select
  - softwareupdate
  - xcodes
  - mas
  - ssh
  - curl
  - sw_vers
  - sysctl
  - top
  - docker
  - podman
  - apt
  - apt-get
  - apt-cache
  - apt-mark
  - dpkg
  categories:
    systemctl: process
    podman: container
    docker: container
    python3: script
    df: storage
    ps: process
    free: system
    uptime: system
    mount: storage
    lsblk: storage
    lsusb: hardware
    journalctl: system
    ss: network
    env: system
    top: process
    lsof: network
    launchctl: macos
    nvidia-smi: gpu
    rocm-smi: gpu
    tailscale: network
    ip: network
    tail: text
    head: text
    cat: text
    ls: storage
    which: system-info
    find: text
    whereis: system-info
    locate: text
    grep: text
    sed: text
    awk: text
    xargs: text
    sort: text
    uniq: text
    cut: text
    wc: text
    git: git
    du: storage
    whoami: system-info
    id: system-info
    uname: system-info
    hostname: system-info
    lspci: hardware
    lsmod: hardware
    modinfo: hardware
    lsmem: hardware
    lscpu: hardware
    jq: json
    yq: json
    dig: network
    nslookup: network
    host: network
    resolvectl: network
    systemd-analyze: system
    file: text
    stat: text
    readlink: text
    realpath: text
    lsb_release: system-info
    vm_stat: macos
    diskutil: macos
    system_profiler: macos
    scutil: macos
    netstat: network
    pgrep: process
    pkill: process
    nohup: process
    brew: package
    xcode-select: macos
    softwareupdate: macos
    xcodes: macos
    mas: package
    ssh: network
    curl: network
    sw_vers: macos
    sysctl: macos
    apt: package
    apt-get: package
    apt-cache: package
    apt-mark: package
    dpkg: package

  caveats:
  - title: systemctl root unit auth hang
    text: Non-interactive mcp_raw calls cannot answer polkit or sudo prompts, so `systemctl` commands targeting root units (e.g. `systemctl cat <unit>` or `systemctl status <unit>`) can appear to hang indefinitely. For user services use `systemctl --user <command>`; for root unit files use `cat /etc/systemd/system/<unit>` or `cat /etc/systemd/system/<unit>.d/override.conf` directly.
  - title: interactive latency and cancellation
    text: mcp_debug commands carry SSH setup overhead and are not cancellable mid-flight; they can appear to hang and be canceled by the user. For fast, read-only, or API-probe steps (e.g. listing Gemini models, local file checks), prefer local bash/ssh on the control host. Reserve mcp_debug for compact, multi-host snapshots or when its compact-output wrappers are specifically needed.
