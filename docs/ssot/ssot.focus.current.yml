title: Current Focus
subtitle: Active shared and branch focuses plus quick wins — read this at the start of every session
icon: target
workflow:
- Assistant must read this SSOT at the start of each session - Read the most recent entry in docs/ssot/ssot.focus.sessions.yml
  to pick up pending follow-ups
- Before any docs/ or SSOT read/grep, run `mcp_call_tool mddb semantic_search` on the request topic and record the query
  in the active focus's request_log
- List docs/ssot/focus-inbox/*.yml at session start and summarize any pending items for triage
- Match the user request against active shared and branch focuses first
- If it matches, append it to that item's request_log and do the work inside that focus
- If it is a small standalone action, add it to quick_wins and complete it immediately
- If it is new strategic work, add it to the backlog in ssot.focus.yml with priority and ask the user to activate it
- If it is a new focus item from another session, add it as an inbox file in docs/ssot/focus-inbox/ for triage
- For backlog review, archived focuses, or focus history, read the full ssot.focus.yml
- At session end, update subtask status and append a session summary to ssot.focus.sessions.yml
- When an inbox item is activated, merge it into this file and git mv the source file to docs/ssot/focus-inbox/processed/
- If the disposition is unclear, call mcp_focus(request, mode=recommend) and follow the returned action (continue, quick_win,
  triage_backlog, triage_inbox, draft_inbox); see ssot.mcp-focus.yml for the machine-readable tree
mcp_focus:
  tool: mcp_focus
  source: docs/ssot/infrastructure/ssot.mcp-focus.yml
  use_when: 'At session start or when the user request is ambiguous. Call mcp_focus with the user

    request and mode=recommend; use the returned action as the first disposition.

    mode=status is a read-only way to list active foci and quick wins.

    '
  actions:
    continue: The request matches an active focus or no request was given; keep working on the active focus.
    quick_win: The request is small and concrete; add to Quick Wins and complete immediately.
    triage_backlog: The request matches or should go to ssot.focus.yml Backlog; ask the user to activate.
    triage_inbox: The request matches an unprocessed focus-inbox draft; review/activate it.
    draft_inbox: No match; return an in-memory draft inbox YAML for the user to review before saving.
decision_tree:
  question: How should this user request be disposed?
  note: 'The canonical machine-readable tree is in docs/ssot/infrastructure/ssot.mcp-focus.yml.

    This tree is the assistant-level guide; each case maps to the mcp_focus action listed below.

    '
  cases:
  - case: matches_active
    mcp_focus_action: continue
    condition: Request matches an active shared/branch focus label, subtask, or quick win
    action: Append to the item's request_log and do the work inside that focus
    examples:
    - let's process focus matched to Service Failures Detected and then Security Vulnerabilities Found
    - do all the streamlining suggestions matched to Streamline focus-driven workflow
  - case: quick_win
    mcp_focus_action: quick_win
    condition: Meets all quick_win_criteria below
    action: Add to Quick Wins, complete immediately, and append to Focus History at session end
    examples:
    - Reduce focus.current bloat
    - Tony-Dell wired DHCP release and renew
  - case: live_bug_report
    mcp_focus_action: continue
    condition: User reports an error in a deployed page or app and asks to add it to focus
    action: Activate as a high-priority branch focus; record the exact URL/error and the chosen fix. Do not default to inbox.
    examples:
    - mcp_debug H3 page mixed-content (HTTP/HTTPS) error
  - case: new_strategic_work
    mcp_focus_action: triage_backlog
    condition: Multi-step or multi-session work that requires planning and tracking
    action: Add to Backlog in ssot.focus.yml with priority; do not start until user activates
    examples:
    - Streamline focus-driven workflow
    - Request-to-Focus Workflow redesign
  - case: side_session_or_ambiguous
    mcp_focus_action: draft_inbox
    condition: New idea from another session or request is vague and needs triage
    action: Save as docs/ssot/focus-inbox/<session>.yml with status draft and missing_info
    examples:
    - Service Failures Detected from overnight assessment
    - Security Vulnerabilities Found from overnight assessment
  - case: auto_generated_alert
    mcp_focus_action: n/a
    condition: Auto-generated item from overnight assessment with missing details
    action: Gather facts with allowed MCP tools, classify as hard or transient, then fix or document accepted state
    examples:
    - Service Failures Detected investigated with mcp-health; .local health-check timeouts, not hard failures
    - Security Vulnerabilities Found had no actionable scan output; documented no immediate patches required
  - case: interrupt
    mcp_focus_action: continue
    condition: New strategic work that must start now while an active shared focus is still incomplete
    action: Park the active shared focus, activate the new work as a branch focus, and return to the parked focus when the
      branch is completed
    examples:
    - Reviewing and implementing focus validation/parking while the mcp-debug shared focus stays parked
  - case: no_active_or_inbox
    mcp_focus_action: triage_backlog or triage_inbox
    condition: No active focus and no inbox items
    action: Suggest the highest triage_score backlog item in reports/NEXT_FOCUS.md; do not auto-activate unless requested
    examples:
    - Dispatcher suggests MacBook macOS App Development when the queue is empty
  - case: requires_mcp_data
    mcp_focus_action: n/a
    condition: Decision needs health, system, or infrastructure data
    action: Use the relevant MCP server per policy, document source, and do not take destructive actions without user confirmation
    examples:
    - mcp-health used for Service Failures investigation
quick_win_criteria:
- Single file or single small edit set
- No new dependencies, installs, or infrastructure changes
- No deploy, restart, or destructive operation required
- Can be verified by a single command or small test
- Completable in under 5 minutes of assistant time
- User has not asked for a decision tree or design review
validation:
  max_active_shared_focus: 1
  max_active_branch_focus: 1
  require_priority: true
  require_tags: true
  require_request_log: true
sections:
- title: Active Shared Focus
  icon: active
  layout: list
  items: []
- title: Active Branch Focus
  icon: branch
  layout: list
  items:
  - label: Trade Canvas UI fixes and modularization
    text: "Continue the work from the 2026-08-15 trade session. Two high-priority\nUI bugs (initial \"Disconnected\" status\
      \ and selection state persistence)\nwere resolved. The remaining in-progress items need triage and\nactivation:\n  -\
      \ Fix the 1 Week timeframe chart rendering for GOLD and other symbols.\n  - Continue the compare/compare2 SSOT-driven\
      \ modularization.\n  - Validate and extend the data source reliability catalog.\n  - Complete the Perfect strategy benchmarking\
      \ with full historical data.\n"
    status: parked
    priority: high
    started: '2026-08-16'
    parked: '2026-08-16'
    estimated_duration: 1 session
    tags:
    - trade
    - ui
    - compare
    - ssot
    - data-sources
    - strategies
    subtasks:
    - label: Reproduce the GOLD 1W empty chart and inspect data / renderer logic
      status: completed
    - label: Add short-timeframe fallback zoom / density handling in chart-renderer.js
      status: completed
    - label: Continue compare/compare2 modularization (promote-feature.sh, generic SSOT patterns)
      status: not_started
    - label: Integrate Frankfurter / exchangerate.dev and validate THB/commodity data paths
      status: not_started
    - label: Run Perfect strategy auto-detect and benchmark against existing strategies
      status: not_started
    request_log:
    - date: '2026-08-16'
      request: Parked to return to focus system modularization.
      matched_to: Trade Canvas UI fixes and modularization
    branch: trade
    source: /home/tony/CascadeProjects/chaba/docs/ssot/focus-inbox/2026-08-15-trade-canvas-continued.yml
  - label: mcp-debug file & clipboard bridge
    text: "Extend mcp-debug with small, auditable file-transfer tools so the assistant can\nfetch remote logs/configs and\
      \ push small scripts without leaving the MCP workflow.\nClipboard support is out-of-scope for the first pass and should\
      \ be added as a\nseparate, experimental tool after file transfer is stable.\n\nProposed design for the complicated bits\
      \ (so this session does not need to solve them again):\n- `mcp_get_file(host, path, max_bytes=65536)` returns `{ok,\
      \ encoding, content_base64, size, truncated}`.\n  Transport: existing SSH exec with `cat` or `head -c`, base64-encode\
      \ the payload.\n- `mcp_put_file(host, path, content_base64, mode=\"644\", overwrite=false)` returns `{ok, path, bytes_written}`.\n\
      \  Transport: existing SSH exec with a heredoc after path-allowlist vetting.\n- `mcp_clipboard_get(host)` / `mcp_clipboard_set(host,\
      \ text)` are macOS `pbpaste`/`pbcopy`\n  or Linux `xclip`/`xsel`/`wl-clipboard`. Disabled by default; per-host opt-in\
      \ only.\n\nSecurity defaults:\n- Per-host `allowed_path_prefixes` (e.g. `~/.local`, `/tmp`, `~/.config`, project dirs).\n\
      - Hard `max_bytes` cap and a `truncated` flag.\n- `overwrite=false` by default; confirmation required for writes outside\
      \ the allow-list.\n- All get/put calls logged with host, path, size, and result.\n"
    status: parked
    priority: medium
    started: '2026-08-16'
    parked: '2026-08-16'
    estimated_duration: 1 session
    tags:
    - mcp
    - mcp-debug
    - file-transfer
    - remote-ops
    - security
    subtasks:
    - label: Promote this design to a dedicated SSOT (docs/ssot/infrastructure/ssot.mcp-debug.files.yml)
      status: not_started
    - label: Add mcp_get_file and mcp_put_file to the mcp-debug server
      status: not_started
    - label: Implement path-allowlist and size-limit vetting
      status: not_started
    - label: Update ssot.mcp-debug.yml with new tools, limits, and host allowlists
      status: not_started
    - label: Add smoke tests for get/put round-trip on tony-omen and tony-dell
      status: not_started
    - label: Design and optionally add mcp_clipboard_get/set as experimental
      status: not_started
    request_log:
    - date: '2026-08-16'
      request: add all into focus and finish before closing
      matched_to: mcp-debug file & clipboard bridge
    - date: '2026-08-16'
      request: let's switch to mcp-focus
      matched_to: mcp-debug file & clipboard bridge
    branch: chaba
    source: /home/tony/CascadeProjects/chaba/docs/ssot/focus-inbox/2026-08-16-061308-mcp-debug-file-bridge.yml
  - label: mcp-debug visual capture bridge
    text: "Extend mcp-debug with lightweight, opt-in visual capture tools so the assistant\ncan see remote host state without\
      \ switching to PlayLive or another MCP server.\nStart with non-interactive capture tools; avoid duplicating PlayLive's\
      \ browser\nautomation and click/keyboard control.\n\nProposed design for the complicated bits (so this session does\
      \ not need to solve them again):\n- `mcp_screenshot(host, region, format=png)` returns `{ok, content_base64, width,\
      \ height, bytes, error}`.\n  Transport: macOS `screencapture -x -`; Linux `gnome-screenshot -f -`, `grim`, or `import`;\n\
      \  always base64-encode and cap at 2 MB (resize/compress if larger).\n- `mcp_window_list(host)` returns `{ok, windows:\
      \ [{id, app, title, pid, focused}]}`.\n  Transport: macOS `lsappinfo`/`osascript`; X11 `xwininfo -root -tree`; Wayland\
      \ `swaymsg -t get_tree`.\n- `mcp_clipboard_image_get(host)` returns `{ok, content_base64, format, bytes, error}`.\n\
      \  Transport: macOS `pbpaste -Prefer rtf` image path/NSPasteboard read; Linux `xclip -selection clipboard -t image/png\
      \ -o`.\n- `mcp_a11y_tree(host, app)` returns a JSON UI element tree of a given app/window.\n  Transport: macOS AppleScript\
      \ accessibility APIs; optional for later.\n\nSecurity defaults:\n- Per-host `capture: true` opt-in; default false on\
      \ all hosts.\n- `mcp_screenshot` must support `region` cropping to avoid leaking full displays.\n- Hard cap on image\
      \ size (2 MB default, 4 MB hard) with downscale for oversize.\n- All capture calls logged with host, type, region, and\
      \ size.\n- clipboard-image is opt-in alongside clipboard text; never called automatically.\n\nImplementation order:\n\
      1. macbook first (pbcopy/pbpaste + screencapture are safest and well documented).\n2. Add Linux screenshot fallbacks\
      \ only after macOS path is stable.\n3. Do not add click/drag/keyboard automation; that stays in PlayLive.\n"
    status: active
    priority: medium
    started: '2026-08-17'
    estimated_duration: 1 session
    tags:
    - mcp
    - mcp-debug
    - capture
    - screenshot
    - macos
    - visual
    - clipboard
    subtasks:
    - label: Promote this design to a dedicated SSOT (docs/ssot/infrastructure/ssot.mcp-debug.capture.yml)
      status: completed
      note: 'Created docs/ssot/infrastructure/ssot.mcp-debug.capture.yml and registered it in ssot.index.yml'
    - label: Add mcp_screenshot with macOS transport and size cap
      status: completed
      note: macOS transport implemented; Linux fallbacks pending
    - label: Add mcp_window_list for macOS and Linux
      status: completed
      note: macOS osascript and Linux X11/Wayland fallbacks implemented; tested on tony_dell (headless, returns empty)
    - label: Add mcp_clipboard_image_get for macOS
      status: completed
      note: macOS osascript and Linux xclip implemented; clipboard_image opt-in per host
    - label: Update ssot.mcp-debug.yml with capture tools, per-host flags, and limits
      status: completed
      note: Capture allowlist and limits live in ssot.mcp-debug.capture.yml; server.py registers the tools
    - label: Add smoke tests on macbook and one Linux host
      status: partial
      note: Linux smoke tests on tony_dell passed (window list OK; screenshot fails with no display; clipboard not enabled); macbook is reachable, but macOS screencapture returns empty and osascript window-list hangs unless a GUI session is active
    request_log:
    - date: '2026-08-17'
      request: "suggest next long action please, I'll be away again"
      matched_to: mcp-debug visual capture bridge
      matched_to: SSOT file optimization
    - date: '2026-08-17'
      request: implement the mcp-debug visual capture bridge
      matched_to: mcp-debug visual capture bridge
    branch: chaba
    source: /home/tony/CascadeProjects/chaba/docs/ssot/focus-inbox/2026-08-17-mcp-debug-visual-capture.yml
- title: Quick Wins
  icon: quick
  layout: list
  items:
  - label: Reduce focus.current bloat
    text: Trimmed completed active focus items from this SSOT; full history remains in ssot.focus.yml.
    status: completed
    priority: high
    tags:
    - ssot
    - focus
    - bloat
    - token-optimization
  - label: Add json and csv output formats to mcp_report
    text: mcp_report now supports json and csv; markdown remains default. Future formats (sjson, ssot, totals_only) recorded
      in ssot.mcp-debug.reports.yml.
    status: completed
    priority: high
    tags:
    - mcp
    - report
    - json
    - csv
    - quick-win
  - label: Add html output format to mcp_report
    text: mcp_report now supports html with styled per-host tables and negative savings highlighted in red.
    status: completed
    priority: high
    tags:
    - mcp
    - report
    - html
    - quick-win
  - label: Commit MDDB semantic search fix
    text: Committed MDDB tooling fixes (chaba-* collections, 127.0.0.1 health checks, curl timeouts) and focus quick win.
    status: completed
    priority: high
    tags:
    - mcp
    - mddb
    - commit
    - quick-win
config:
  version: 2
  last_updated: '2026-08-17'
  maintainer: tony
  full_focus_file: docs/ssot/ssot.focus.yml
  inbox_dir: docs/ssot/focus-inbox
  inbox_processed_dir: docs/ssot/focus-inbox/processed
  validation_enabled: true
  related_files:
  - docs/ssot/ssot.focus.yml
  - docs/ssot/ssot.focus.decisions.yml
  - docs/ssot/infrastructure/ssot.focus.triage.yml
  - docs/ssot/ssot.index.yml
  - docs/ssot/focus-inbox
  - docs/ssot/infrastructure/ssot.subagent-focus-triage.yml
  - docs/ssot/infrastructure/ssot.focus-dispatcher.yml
