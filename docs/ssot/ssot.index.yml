title: SSOT Index
subtitle: Master index of all SSOT files — purpose, location, and key content
icon: 🗂️

ideas:
  - Single entry point for orienting to the SSOT system
  - Use ssot-search skill to search across all SSOT content (YAML only) or all documentation (MCP docs server)
  - Use ssot-validate skill to check all files for errors
  - See docs/kb/documentation-search.md for dual search methods guide
  - Files without subtitles are config-type (flexible structure, no title required)
  - SSOT summaries in docs/ssot-summaries/ provide searchable overviews for MCP docs server

sections:
  - title: Core / Cross-Project
    icon: 🌐
    layout: list
    items:
      - label: ssot.index.yml
        text: This file. Master index of all SSOT files.
        path: docs/ssot/ssot.index.yml

      - label: ssot.focus.yml
        text: Strategic focus areas — shared vs per-branch, active/pending/completed with dependency tracking and history
        path: docs/ssot/ssot.focus.yml

      - label: ssot.focus.current.yml
        text: Active focus areas and quick wins — lightweight .current view for session starts
        path: docs/ssot/ssot.focus.current.yml

      - label: ssot.improvements.yml
        text: Pending system improvements with priority, effort, dependencies, and impact scoring. Active items only.
        path: docs/ssot/ssot.improvements.yml

      - label: ssot.improvements.archive.yml
        text: Completed improvements, archived from ssot.improvements.yml to reduce noise
        path: docs/ssot/ssot.improvements.archive.yml

      - label: ssot.validation-patterns.yml
        text: Validation rules, file-type classifications, and best practices for the SSOT system itself
        path: docs/ssot/ssot.validation-patterns.yml

      - label: ssot.maintenance.yml
        text: SSOT maintenance framework — systematic schedule, quality metrics, change tracking, and automation tools
        path: docs/ssot/ssot.maintenance.yml

      - label: ssot.file-control.yml
        text: SSOT file control — naming, placement, change workflow, validation, and search conventions
        path: docs/ssot/ssot.file-control.yml

      - label: CHANGELOG.md
        text: SSOT change log — tracking significant changes, audit trail, and rollback capability for all SSOT modifications
        path: docs/ssot/CHANGELOG.md

      - label: ssot.token-optimization.yml
        text: MCP filter config, Headroom proxy, Devin usage patterns — strategies for reducing AI token costs
        path: docs/ssot/ssot.token-optimization.yml

  - title: System & Environment
    icon: 🖥️
    layout: list
    items:
      - label: ssot.mysystem.home.yml
        text: Home environment — workstations (tony-omen, tony-dell), hardware specifications, network, services, health check URLs
        path: docs/ssot/ssot.mysystem.home.yml

      - label: ssot.mysystem.mobile.yml
        text: Mobile/remote environment — device setup and service access from outside home network
        path: docs/ssot/ssot.mysystem.mobile.yml

      - label: ssot.mysystem.macbook.yml
        text: MacBook Pro workstation — system configuration, remote access (Chrome Remote Desktop + SSH), network integration
        path: docs/ssot/ssot.mysystem.macbook.yml

      - label: ssot.mysystem.macbook-appdev.yml
        text: MacBook Pro app development target state — Xcode, signing, simulators, and phased plan
        path: docs/ssot/ssot.mysystem.macbook-appdev.yml

      - label: infrastructure/ssot.health.yml
        text: Health check endpoint definitions and recovery guidance for all services with profile-based configuration (home/mobile). Includes CPU frequency monitoring and throttling detection.
        path: docs/ssot/infrastructure/ssot.health.yml

      - label: infrastructure/ssot.mcp.yml
        text: MCP infrastructure SSOT — server definitions, profile switching, configuration generation. Includes mcp-health server for centralized health monitoring.
        path: docs/ssot/infrastructure/ssot.mcp.yml

      - label: infrastructure/ssot.mcp-tools.yml
        text: MCP tools policy and catalog — selection rules, fallbacks, and failure procedures
        path: docs/ssot/infrastructure/ssot.mcp-tools.yml

      - label: infrastructure/ssot.gpu.yml
        text: GPU policy, VRAM budget, queue implementation details, systemd services, and MCP tool reference
        path: docs/ssot/infrastructure/ssot.gpu.yml

      - label: infrastructure/ssot.services.yml
        text: Service configuration summary — llama-router, postgres, and container details
        path: docs/ssot/infrastructure/ssot.services.yml

      - label: infrastructure/ssot.gemini-models.yml
        text: Canonical Google Gemini/Gemma model registry with free-tier rate limits and usage assignments
        path: docs/ssot/infrastructure/ssot.gemini-models.yml

      - label: infrastructure/ssot.automation.yml
        text: Automated monitoring and maintenance schedules — GPU monitoring, system maintenance, overnight assessment
        path: docs/ssot/infrastructure/ssot.automation.yml

      - label: infrastructure/ssot.disaster-recovery.yml
        text: Disaster recovery infrastructure — backup scripts, recovery procedures, verification status, automation schedule
        path: docs/ssot/infrastructure/ssot.disaster-recovery.yml

      - label: infrastructure/ssot.tony-dell-monitoring.yml
        text: Tony-Dell 24/7 monitoring and recovery plan — persistent cross-host health checks and safe auto-recovery policy
        path: docs/ssot/infrastructure/ssot.tony-dell-monitoring.yml

      - label: infrastructure/ssot.link-monitor.yml
        text: TCP/IP link monitor — reachability watchdog with cheap probes, escalated scans, and safe fix attempts for tony-omen, tony-dell, and macbook
        path: docs/ssot/infrastructure/ssot.link-monitor.yml

      - label: infrastructure/ssot.mcp-debug.cors.yml
        text: MCP Debug CORS endpoint — policy for the live mcp-savings.json HTTP endpoint on tony-omen
        path: docs/ssot/infrastructure/ssot.mcp-debug.cors.yml

      - label: infrastructure/ssot.mcp-focus.yml
        text: MCP Focus — session-start router and focus recommendation policy for the mcp_focus tool
        path: docs/ssot/infrastructure/ssot.mcp-focus.yml

      - label: infrastructure/ssot.host-roles.yml
        text: Host roles — canonical roles, headless priority, and web preview targets for chaba hosts
        path: docs/ssot/infrastructure/ssot.host-roles.yml

      - label: infrastructure/ssot.containerization.yml
        text: Service containerization plans and progress — host services migration to Docker, resource management improvements
        path: docs/ssot/infrastructure/ssot.containerization.yml

  - title: UI & Frontend
    icon: 🎨
    layout: list
    items:
      - label: ssot.libs.yml
        text: Design tokens, shared components (nav, app cards), asset paths, build workflow, and constraints
        path: docs/ssot/ssot.libs.yml

      - label: ssot.ui.yml
        text: Stub — redirects to ssot.libs.yml for the authoritative UI reference
        path: docs/ssot/ssot.ui.yml

      - label: ssot.diagrams.yml
        text: Architecture and connection diagrams for Playlive, Barrier, and other multi-host setups
        path: docs/ssot/ssot.diagrams.yml

  - title: Applications
    icon: 📱
    layout: list
    items:
      - label: apps/ssot.apps.yml
        text: App registry — all app names and per-host deployment mappings
        path: docs/ssot/apps/ssot.apps.yml

      - label: apps/ssot.apps.raceman.yml
        text: Raceman race management system — course editor, simulation, and visualization
        path: docs/ssot/apps/ssot.apps.raceman.yml

      - label: apps/ssot.apps.playlive.yml
        text: Playlive browser automation — consolidated daemon, local/remote sessions, tools, systemd reliability
        path: docs/ssot/apps/ssot.apps.playlive.yml

      - label: apps/ssot.apps.track4.yml
        text: Track4 course simulator — modularization, features, and testing
        path: docs/ssot/apps/ssot.apps.track4.yml

      - label: apps/ssot.apps.aihub.yml
        text: AI Hub — multi-AI browser automation via Playlive
        path: docs/ssot/apps/ssot.apps.aihub.yml

      - label: apps/ssot.apps.chaba.yml
        text: Main chaba application and infrastructure overview
        path: docs/ssot/apps/ssot.apps.chaba.yml

      - label: apps/ssot.apps.map3d.yml
        text: Map3D 3D map viewer — point clouds, Gaussian splats, tiltable overlays
        path: docs/ssot/apps/ssot.apps.map3d.yml

      - label: apps/ssot.apps.wind.yml
        text: Wind forecast page for Track4 course locations
        path: docs/ssot/apps/ssot.apps.wind.yml

      - label: apps/ssot.apps.cams.yml
        text: Camera monitor application
        path: docs/ssot/apps/ssot.apps.cams.yml

      - label: apps/ssot.apps.imagen2.yml / apps/ssot.apps.imagen3.yml
        text: Image generation service configurations
        path: docs/ssot/apps/ssot.apps.imagen2.yml

      - label: apps/ssot.apps.deka.yml
        text: Deka application
        path: docs/ssot/apps/ssot.apps.deka.yml

      - label: apps/ssot.apps.test-pwa.yml
        text: Test PWA — Progressive Web App demo for iPad testing with full-screen support
        path: docs/ssot/apps/ssot.apps.test-pwa.yml

      - label: apps/ssot.apps.test-carplay.yml
        text: Test CarPlay — CarPlay-style simulation interface for iPad testing with GPS tracking and route planning
        path: docs/ssot/apps/ssot.apps.test-carplay.yml

  - title: Tooling & Knowledge
    icon: 🔧
    layout: list
    items:
      - label: ssot.devin.tools.yml
        text: MCP server definitions and launch configuration for Devin/Windsurf (config-type)
        path: docs/ssot/ssot.devin.tools.yml

      - label: ssot.docs.yml
        text: Document processing tools, LLM infrastructure, and interesting libraries to evaluate
        path: docs/ssot/ssot.docs.yml

      - label: ssot.test.weaviate.yml
        text: Weaviate vector database — embedding service setup, test plans, and optimization notes
        path: docs/ssot/ssot.test.weaviate.yml

      - label: mcp/mcp-health/
        text: MCP health monitoring server — centralized health checks, historical tracking, dependency analysis, recovery suggestions, alert system, port conflict detection, proxy validation, automated service recovery
        path: mcp/mcp-health/

      - label: mcp-health-vs-old-skill-comparison.md
        text: Feature comparison between MCP health server and deprecated health-check skill with parity assessment and migration justification
        path: docs/kb/mcp-health-vs-old-skill-comparison.md

      - label: hibernation-troubleshooting.md
        text: Hibernation troubleshooting guide — kernel parameter mismatch, swap configuration, verification commands
        path: docs/kb/hibernation-troubleshooting.md

  - title: Templates
    icon: 📋
    layout: list
    items:
      - label: template.yml
        text: Canonical template for creating new SSOT files — copy and fill in
        path: docs/ssot/template.yml

      - label: apps/template.app.yml
        text: Standardized template for application SSOT files — ensures consistency across app documentation
        path: docs/ssot/apps/template.app.yml

  - title: Trade System
    icon: 📊
    layout: list
    items:
      - label: Trade SSOT Index
        text: Trade system SSOT index — health, infrastructure, data sources, API, signals, backtesting, UI
        path: /home/tony/CascadeProjects/trade/config/ssot/ssot.index.yml

      - label: Trade Health SSOT
        text: Health check endpoints and monitoring for trade services — API, automation, database
        path: /home/tony/CascadeProjects/trade/config/ssot/ssot.health.yml

      - label: Trade Infrastructure SSOT
        text: Trade system infrastructure — Docker Compose, networks, machines, deployment
        path: /home/tony/CascadeProjects/trade/config/ssot/ssot.infrastructure.yml

      - label: Trade Data Sources SSOT
        text: Data source configuration — APIs, download URLs, scheduling, validation
        path: /home/tony/CascadeProjects/trade/config/ssot/ssot.data.yml

  - title: Summaries
    icon: 📝
    layout: list
    items:
      - label: ssot-summaries/ssot.core-summary.md
        text: Core SSOT configuration overview for searchability — index, focus, improvements, validation
        path: docs/ssot-summaries/ssot.core-summary.md

      - label: ssot-summaries/ssot.apps-summary.md
        text: Apps registry and individual app SSOT overviews — track4, raceman, wind, map3d, test-pwa
        path: docs/ssot-summaries/ssot.apps-summary.md

      - label: ssot-summaries/ssot.infrastructure-summary.md
        text: Infrastructure SSOT overviews — health, GPU, services, automation, disaster recovery
        path: docs/ssot-summaries/ssot.infrastructure-summary.md

      - label: ssot-summaries/ssot.gpu-summary.md
        text: GPU policy and queue implementation — VRAM budget, models, backpressure system
        path: docs/ssot-summaries/ssot.gpu-summary.md

      - label: ssot-summaries/ssot.health-summary.md
        text: Health check configuration — service definitions, recovery actions, location-specific configs
        path: docs/ssot-summaries/ssot.health-summary.md

config:
  version: 1
  last_updated: 2026-08-06
  maintainer: tony
  notes: Update this index when adding or renaming SSOT files
