# Package: pool_sankey
# Add this under `homeassistant: packages:` as:
#   pool_sankey: !include packages/pool_sankey.yaml

utility_meter:
  pool_energy_daily:
    source: sensor.pool_wifi_smart_meter_switch_total_energy
    cycle: daily
