server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve the app under /jarvis/
  location = /jarvis {
    return 301 /jarvis/;
  }

  location ^~ /jarvis/ {
    try_files $uri $uri/ /jarvis/index.html;
  }
}
