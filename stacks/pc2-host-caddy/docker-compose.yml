services:
  host-caddy:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "10.8.0.12:80:80"
      - "10.8.0.12:443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    environment:
      CADDY_ADMIN: "0.0.0.0:2019"

volumes:
  caddy-data:
  caddy-config:
