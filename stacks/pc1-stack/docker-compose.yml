version: "3.9"

x-default-service: &default-service
  restart: unless-stopped
  env_file:
    - .env
  networks:
    - pc1-stack-net
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"

services:
  mcp0:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP0_BUILD_CONTEXT:-../../mcp/mcp0}
    ports:
      - "${MCP0_PORT:-8351}:${MCP0_PORT:-8351}"
    environment:
      MCP0_APP_NAME: ${MCP0_APP_NAME:-pc1-mcp0}
      MCP0_PORT: ${MCP0_PORT:-8351}
      MCP0_ADMIN_TOKEN: ${MCP0_ADMIN_TOKEN:-}
      MCP0_PROVIDERS: ${MCP0_PROVIDERS:-}
      MCP0_ALLOW_ORIGINS: ${MCP0_ALLOW_ORIGINS:-http://localhost:3000,http://dev-host.pc1:3100}
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${MCP0_PORT:-8351}/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  mcp-agents:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP_AGENTS_BUILD_CONTEXT:-../../mcp/mcp-agents}
    ports:
      - "${MCP_AGENTS_PORT:-8046}:8046"
    environment:
      PORT: ${MCP_AGENTS_PORT:-8046}
      AGENTS_API_BASE: ${AGENTS_API_BASE:-http://dev-host.pc1:3100/test/agents/api}
      AGENTS_DEFAULT_USER: ${AGENTS_DEFAULT_USER:-default}
      AGENTS_DEFAULT_LIMIT: ${AGENTS_DEFAULT_LIMIT:-12}
      JSON_BODY_LIMIT: ${AGENTS_JSON_LIMIT:-2mb}
    volumes:
      - ${MCP_AGENTS_DATA_DIR:-./data/mcp-agents}:/app/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8046/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  mcp-glama:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP_GLAMA_BUILD_CONTEXT:-../../mcp/mcp-glama}
    ports:
      - "${MCP_GLAMA_PORT:-7241}:8014"
    environment:
      PORT: 8014
      GLAMA_API_URL: ${GLAMA_API_URL:-}
      GLAMA_API_KEY: ${GLAMA_API_KEY:-}
      GLAMA_MODEL: ${GLAMA_MODEL:-gpt-4o-mini}
      GLAMA_TEMPERATURE: ${GLAMA_TEMPERATURE:-0.2}
      GLAMA_MAX_TOKENS: ${GLAMA_MAX_TOKENS:-900}
      GLAMA_TIMEOUT_SECONDS: ${GLAMA_TIMEOUT_SECONDS:-30}
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8014/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  mcp-tester:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP_TESTER_BUILD_CONTEXT:-../../mcp/mcp-tester}
    ports:
      - "${MCP_TESTER_PORT:-8335}:${MCP_TESTER_PORT:-8335}"
    environment:
      MCP_TESTER_HOST: 0.0.0.0
      MCP_TESTER_PORT: ${MCP_TESTER_PORT:-8335}
      MCP_TESTER_SUITE_FILE: ${MCP_TESTER_SUITE_FILE:-/app/tests.example.json}
      MCP_TESTER_SUITE_FILES: ${MCP_TESTER_SUITE_FILES:-/app/tests.optional.json}
      MCP_TESTER_DEFAULT_TIMEOUT_MS: ${MCP_TESTER_DEFAULT_TIMEOUT_MS:-5000}
      MCP_TESTER_HISTORY_FILE: ${MCP_TESTER_HISTORY_FILE:-/data/run-history.ndjson}
      DEV_HOST_TEST_BASE: ${DEV_HOST_TEST_BASE:-http://dev-host.pc1:3000}
      A1_IDC1_BASE_URL: ${A1_IDC1_BASE_URL:-https://a1.idc1.surf-thailand.com}
      A1_IDC1_TEST_URL: ${A1_IDC1_TEST_URL:-https://a1.idc1.surf-thailand.com/test}
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${MCP_TESTER_PORT:-8335}/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    volumes:
      - ${MCP_TESTER_DATA_DIR:-./data/mcp-tester}:/data

  mcp-devops:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP_DEVOPS_BUILD_CONTEXT:-../../mcp/mcp-devops}
    ports:
      - "${MCP_DEVOPS_PORT:-8325}:${MCP_DEVOPS_PORT:-8325}"
    environment:
      MCP_DEVOPS_PORT: ${MCP_DEVOPS_PORT:-8325}
      MCP_DEVOPS_HOST: 0.0.0.0
      MCP_DEVOPS_REPO_ROOT: /workspaces/chaba
      DEV_HOST_BASE_URL: ${DEV_HOST_BASE_URL:-http://dev-host.pc1}
      DEV_HOST_PC2_BASE_URL: ${DEV_HOST_PC2_BASE_URL:-http://dev-host.pc2:3000}
      MCP0_BASE_URL: ${MCP0_BASE_URL:-http://mcp0:${MCP0_PORT:-8351}}
      MCP0_ADMIN_TOKEN: ${MCP0_ADMIN_TOKEN:-}
      MCP_DEVOPS_JSON_LIMIT: ${MCP_DEVOPS_JSON_LIMIT:-2mb}
    depends_on:
      - mcp0
    volumes:
      - ../../:/workspaces/chaba:cached
      - //var/run/docker.sock:/var/run/docker.sock

  mcp-playwright:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP_PLAYWRIGHT_BUILD_CONTEXT:-../../mcp/mcp-playwright}
    ports:
      - "${MCP_PLAYWRIGHT_PORT:-8260}:${MCP_PLAYWRIGHT_PORT:-8260}"
    environment:
      PORT: ${MCP_PLAYWRIGHT_PORT:-8260}
      PLAYWRIGHT_BROWSER: ${PLAYWRIGHT_BROWSER:-chromium}
      PLAYWRIGHT_HEADLESS: ${PLAYWRIGHT_HEADLESS:-true}
      PLAYWRIGHT_TIMEOUT_MS: ${PLAYWRIGHT_TIMEOUT_MS:-15000}
      PLAYWRIGHT_SCENARIOS_DIR: /app/scenarios
      PLAYWRIGHT_OUTPUT_DIR: /app/output
    volumes:
      - ${MCP_PLAYWRIGHT_SCENARIOS_DIR:-./data/mcp-playwright/scenarios}:/app/scenarios
      - ${MCP_PLAYWRIGHT_OUTPUT_DIR:-./data/mcp-playwright/output}:/app/output
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${MCP_PLAYWRIGHT_PORT:-8260}/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  webtop2:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ./webtop2
    container_name: pc1-webtop2
    hostname: webtop2
    shm_size: "1gb"
    ports:
      - "${WEBTOP2_PORT:-3003}:3000"
    environment:
      PUID: ${WEBTOP_PUID:-1000}
      PGID: ${WEBTOP_PGID:-1000}
    volumes:
      - webtop2-config:/config
      - ../idc1-stack/webtop/custom-cont-init.d:/custom-cont-init.d:ro
      - ../../:/workspaces/chaba:cached
    healthcheck:
      test: ["CMD-SHELL", "bash -lc 'DISPLAY=${DISPLAY:-:1} xrandr --query >/dev/null 2>&1'"]
      interval: 30s
      timeout: 5s
      retries: 3

  webtop3:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ./webtop2
    container_name: pc1-webtop3
    hostname: webtop3
    shm_size: "1gb"
    ports:
      - "${WEBTOP3_PORT:-3004}:3000"
    environment:
      PUID: ${WEBTOP_PUID:-1000}
      PGID: ${WEBTOP_PGID:-1000}
    volumes:
      - webtop3-config:/config
      - ../idc1-stack/webtop/custom-cont-init.d:/custom-cont-init.d:ro
      - ../../:/workspaces/chaba:cached
    healthcheck:
      test: ["CMD-SHELL", "bash -lc 'DISPLAY=${DISPLAY:-:1} xrandr --query >/dev/null 2>&1'"]
      interval: 30s
      timeout: 5s
      retries: 3

  mcp-webtop2:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP_WEBTOP_BUILD_CONTEXT:-../../mcp/mcp-webtop}
    container_name: pc1-mcp-webtop2
    ports:
      - "${MCP_WEBTOP_PORT:-8055}:8055"
    environment:
      PORT: 8055
      WEBTOP_CONFIG_DIR: /webtop-config
      EXPORT_DIR: /data/exports
      JSON_BODY_LIMIT: ${MCP_WEBTOP_JSON_LIMIT:-1mb}
    volumes:
      - webtop2-config:/webtop-config
      - ${MCP_WEBTOP_DATA_DIR:-./data/mcp-webtop}:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8055/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  mcp-webtop3:
    <<: *default-service
    profiles: ["mcp-suite"]
    build:
      context: ${MCP_WEBTOP_BUILD_CONTEXT:-../../mcp/mcp-webtop}
    container_name: pc1-mcp-webtop3
    ports:
      - "${MCP_WEBTOP3_PORT:-8056}:8056"
    environment:
      PORT: 8056
      WEBTOP_CONFIG_DIR: /webtop-config
      EXPORT_DIR: /data/exports
      JSON_BODY_LIMIT: ${MCP_WEBTOP_JSON_LIMIT:-1mb}
    volumes:
      - webtop3-config:/webtop-config
      - ${MCP_WEBTOP3_DATA_DIR:-./data/mcp-webtop3}:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8056/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  caddy:
    <<: *default-service
    profiles: ["mcp-suite"]
    image: caddy:2
    container_name: pc1-caddy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config

networks:
  pc1-stack-net:
    driver: bridge

volumes:
  webtop2-config:
  webtop3-config:
  caddy-data:
  caddy-config:
