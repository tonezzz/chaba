# pc2-core stack environment template (Core services)
# Copy to .env (kept out of git) and replace secrets marked __REPLACE_ME__.

# Core Runtime Services
HOST_REPO=/home/tonezzz/repos
HOST_SSH_AUTH_SOCK=/home/tonezzz/.ssh/ssh-agent.socket
HOST_PIP_CACHE=/home/tonezzz/.cache/pip

# Redis Configuration
REDIS_PORT=6379

# Development Proxy
DEV_PROXY_CONFIG=./dev-proxy/Caddyfile
DEV_PROXY_CERTS=./dev-proxy/certs
DEV_PROXY_ADMIN_PORT=2019
DEV_HOST_VAJA_TARGET=http://mcp-vaja.pc2-tools-net:8017
