# Copy to .env on idc1 (this file is safe to commit)

# Public endpoint for WireGuard clients to connect to
WG_HOST=idc1.surf-thailand.com
WG_PORT=51820

# wg-easy VPN subnet + DNS
WG_DEFAULT_ADDRESS=10.8.0.x
WG_DEFAULT_DNS=10.8.0.1
WG_ALLOWED_IPS=10.8.0.0/24
WG_PERSISTENT_KEEPALIVE=25

# IMPORTANT: Docker host gateway IP as seen from inside the idc1-wg-easy container.
# Used to DNAT 10.8.0.1:22 -> <host-gateway>:22 via WG_POST_UP.
# If ssh over VPN is "connection refused" and DNAT rules are missing/mis-pointed,
# set this to the default route gateway shown by:
#   docker exec -it idc1-wg-easy sh -lc "ip route | head -n 20"
WG_EASY_HOST_GW=172.20.0.1

# Optional: password for wg-easy UI (served only on 127.0.0.1:51821 by default)
# Leave empty to disable
WG_EASY_PASSWORD=
